1. Install mysql server (https://dev.mysql.com/downloads/installer/)
-   Set root password 
-   add db called eth
-   Create table called wallets and wallets_t
- CREATE TABLE wallets (address VARCHAR(255) NOT NULL, eth_balance DECIMAL(30,0), PRIMARY KEY (address));
2. Install Node
-   npm install
3. Use Google BigQuery to get ethereum address
- SELECT `address`, `eth_balance`
FROM `bigquery-public-data.crypto_ethereum.balances`
WHERE `eth_balance` >  2000000.0e18
ORDER BY `eth_balance` DESC;
- Export csv files to bucket
- Download csv files
- run split_csv.py to split file into smaller chunks
4. Import csv files to mysql
-   run server_utils.js, change directly of where csv files are located
5. Run main.js


LOAD DATA LOCAL INFILE 'C:/Users/jpitt/Downloads/wallet.csv' INTO TABLE wallet_data FIELDS TERMINATED BY ',' ENCLOSED BY '"' LINES TERMINATED BY '\n' IGNORE 1 ROWS (address, eth_balance);